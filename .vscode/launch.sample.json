{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${workspaceRoot}/export.py",
            "console": "integratedTerminal",
            "args": [
                "--api-endpoint=endpoint",
                "--owner=owner",
                "--repo=repo",
                "--token=token"
            ]
        }
    ]
}